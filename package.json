{
  "name": "engineering-notes",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev .",
    "docs:build": "vitepress build . && cp -a public/. .vitepress/dist/ && test -f .vitepress/dist/resume/index.html && test -f .vitepress/dist/resume/sections/career.html && test -f .vitepress/dist/resume/sections/personal.html && ! grep -q 'Promise.all' public/resume/index.html && ! grep -q 'createElement.*template' public/resume/index.html && ! grep -q 'outerHTML' public/resume/index.html",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
